# devel and test utilities

This folder provides a docker-compose.yml to boot a test guacamole instance for development and testing

It also provides some curl examples to interact with the guacamole API
